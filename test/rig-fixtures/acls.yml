# THROWAWAY fixture (test rig only) to exercise apply_acls.py / synoshare --setuser.
# Requires a share `acltest` and group `rigtest` on the rig. Point the role at this dir
# with: -e nas_spec_dir=.../test/rig-fixtures
acls:
  - share: acltest
    grants:
      - { group: rigtest, access: rw }
      - { user: myadmin, access: ro }
